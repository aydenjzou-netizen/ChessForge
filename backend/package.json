{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test test/*.test.js",
    "db:migrate": "node --env-file-if-exists=.env scripts/migrate.js",
    "db:verify": "node --env-file-if-exists=.env scripts/verifyDatabase.js",
    "db:prepare": "npm run db:migrate && npm run storage:import && npm run db:verify",
    "storage:import": "node scripts/importLegacyStorage.js",
    "privacy:maintain": "node scripts/privacyMaintenance.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "body-parser": "^2.2.2",
    "chess.js": "^0.10.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "fs-extra": "^11.3.4",
    "google-auth-library": "^10.9.1",
    "pg": "^8.22.0",
    "uuid": "^14.0.0"
  }
}
